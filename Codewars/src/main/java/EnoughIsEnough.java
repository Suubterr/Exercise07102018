public class EnoughIsEnough {
    public static int[] deleteNth(int[] elements, int maxOccurrences) {
        String result = "";
        for(int i = 0; i < elements.length; i ++) {
            if(result.chars().filter(ch -> ch == elements[i].count())) {
        }
    }
}